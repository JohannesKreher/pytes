# Enjoy
1. Install in the extracted folder within a pyvenv:

```
    cd /path/to/extracted/directory
    python3 -m venv venv
    source pyvenv/bin/activate
    pip install .
```
1. Uninstall:
    pip uninstall software-name
